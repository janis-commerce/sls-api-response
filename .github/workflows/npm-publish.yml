name: NPM Publish

on:
  push:
    tags:
      - '**'

jobs:
  publish:
    uses: janis-commerce/.github/.github/workflows/npm-publish.yml@master
    with:
      slack-username: SLS API Response
    secrets:
      npm_token: ${{ secrets.npm_token }}
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
